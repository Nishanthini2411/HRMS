const Payroll = () => {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Payroll</h1>
      <p className="text-sm text-gray-600">
        Generate monthly payroll, calculate salaries, deductions, and download payslips.
      </p>
      <div className="bg-white rounded-xl border border-dashed border-gray-300 p-6 text-gray-400 text-sm">
        💰 Payroll processing UI (employee salary table, generate payslip button, etc.) can be added here.
      </div>
    </section>
  );
};

export default Payroll;
